"use strict";
/*
 * Copyright (c) "Neo4j"
 * Neo4j Sweden AB [http://neo4j.com]
 *
 * This file is part of Neo4j.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const constants_1 = require("../constants");
function checkNeo4jCompat({ driver, driverConfig }) {
    return __awaiter(this, void 0, void 0, function* () {
        yield driver.verifyConnectivity();
        const sessionParams = {};
        if (driverConfig) {
            if (driverConfig.database) {
                sessionParams.database = driverConfig.database;
            }
            if (driverConfig.bookmarks) {
                sessionParams.bookmarks = driverConfig.bookmarks;
            }
        }
        const session = driver.session(sessionParams);
        const cypher = `
        CALL dbms.components() yield versions
        WITH head(versions) AS version
        CALL dbms.functions() yield name AS functions
        WITH version, COLLECT(functions) AS functions
        CALL dbms.procedures() yield name AS procedures
        RETURN
            version,
            functions,
            COLLECT(procedures) AS procedures,
            CASE "apoc.version" IN functions
                WHEN true THEN apoc.version()
                ELSE false
            END AS apocVersion
    `;
        try {
            const result = yield session.run(cypher);
            const info = result.records[0].toObject();
            if (info.version < constants_1.MIN_NEO4J_VERSION) {
                throw new Error(`Expected minimum Neo4j version: '${constants_1.MIN_NEO4J_VERSION}' received: '${info.version}'`);
            }
            if (info.apocVersion < constants_1.MIN_APOC_VERSION) {
                throw new Error(`Expected minimum APOC version: '${constants_1.MIN_APOC_VERSION}' received: '${info.apocVersion}'`);
            }
            const missingFunctions = constants_1.REQUIRED_APOC_FUNCTIONS.filter((f) => !info.functions.includes(f));
            if (missingFunctions.length) {
                throw new Error(`Missing APOC functions: [ ${missingFunctions.join(", ")} ]`);
            }
            const missingProcedures = constants_1.REQUIRED_APOC_PROCEDURES.filter((p) => !info.procedures.includes(p));
            if (missingProcedures.length) {
                throw new Error(`Missing APOC procedures: [ ${missingProcedures.join(", ")} ]`);
            }
        }
        finally {
            yield session.close();
        }
    });
}
exports.default = checkNeo4jCompat;
//# sourceMappingURL=verify-database.js.map