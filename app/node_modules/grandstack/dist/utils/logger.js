"use strict";

var _interopRequireDefault = require("@babel/runtime-corejs3/helpers/interopRequireDefault");

var _Object$defineProperty = require("@babel/runtime-corejs3/core-js/object/define-property");

_Object$defineProperty(exports, "__esModule", {
  value: true
});

exports.infoExitZero = exports.logData = exports.info = exports.exitWithWarning = exports.exitWithError = void 0;

var _chalk = _interopRequireDefault(require("chalk"));

// Move to enum when convert to TS
// const errorTypes = [`CREDSMISSING`]
// Move to enum when convert to TS
// const warningTypes = [`CREDSMISSING`]
const exitWithError = ({
  tag,
  msg,
  code,
  display = false
}) => {
  if (display) {
    console.log(`%s :: ${msg}`, _chalk.default.redBright(tag));
    process.exit(code);
  }

  console.log(`${tag} :: ${msg}`);
  process.exit(code);
};

exports.exitWithError = exitWithError;

const exitWithWarning = ({
  tag,
  msg,
  code,
  display = false
}) => {
  if (display) {
    console.log(`%s :: ${msg}`, _chalk.default.yellow(tag));
    process.exit(code);
  }

  console.log(`${tag} :: ${msg}`);
  process.exit(code);
};

exports.exitWithWarning = exitWithWarning;

const info = ({
  msg,
  display = false
}) => {
  if (display) {
    console.log(`%s :: ${msg}`, _chalk.default.green("INFO"));
    return;
  }

  console.log(`INFO :: ${msg}`);
};

exports.info = info;

const logData = ({
  idMarker,
  dataString,
  display = false
}) => {
  if (display) {
    console.log(`$${_chalk.default.green(idMarker)}=>${dataString}`);
    return;
  }

  console.log(`$${idMarker}=>${dataString}`);
};

exports.logData = logData;

const infoExitZero = msg => {
  info(msg);
  process.exit(0);
};

exports.infoExitZero = infoExitZero;